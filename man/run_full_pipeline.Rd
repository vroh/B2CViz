% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist_analysis_functions.R
\name{run_full_pipeline}
\alias{run_full_pipeline}
\title{Run the full neighborhood analysis pipeline}
\usage{
run_full_pipeline(
  edges,
  mean_step,
  distance_breaks = c(0, 25, 50, 100, 200, 400, 800, Inf),
  radii = c(25, 50, 100, 200, 400),
  border_trim = FALSE,
  coords = NULL,
  roi_windows = NULL
)
}
\arguments{
\item{edges}{A standardized edge data frame (output of
\code{\link{standardize_get_dist}}).}

\item{mean_step}{Numeric. The median nearest-neighbor step size (in the same
units as \code{distance}) used to rescale the distance breakpoints and
radii. Typically obtained as the \code{step} element of the \code{plot_b2c}
return value.}

\item{distance_breaks}{Numeric vector. Bin breakpoints \emph{before}
multiplication by \code{mean_step}
(default \code{c(0, 25, 50, 100, 200, 400, 800, Inf)}).}

\item{radii}{Numeric vector. Cumulative radii \emph{before} multiplication
by \code{mean_step} (default \code{c(25, 50, 100, 200, 400)}).}

\item{border_trim}{Logical. If \code{TRUE}, remove origin cells closer to
the ROI border than \code{max(radii)} (requires \code{coords} and
\code{roi_windows}).}

\item{coords}{A data frame of cell coordinates required when
\code{border_trim = TRUE}.}

\item{roi_windows}{A data frame of ROI bounding boxes required when
\code{border_trim = TRUE}.}
}
\value{
A named list with elements: \code{qc}, \code{pair_stats},
  \code{radial_stats}, \code{cumulative_stats}, \code{group_pair_stats},
  and \code{cells}.
}
\description{
A convenience wrapper that sequentially calls QC, optional border trimming,
\code{\link{summarize_pairs}}, \code{\link{summarize_radial_bins}},
\code{\link{summarize_cumulative}}, and \code{\link{summarize_groups}}.
}
