% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist_analysis_functions.R
\name{standardize_get_dist}
\alias{standardize_get_dist}
\title{Standardize a combined get_dist() output table}
\usage{
standardize_get_dist(
  df,
  roi_col = "roi",
  group_col = "group",
  origin_id_col = "origin_name",
  origin_type_col = "origin_value",
  neighbor_id_col = "neighbor_name",
  neighbor_type_col = "neighbor_value",
  distance_col = "distance"
)
}
\arguments{
\item{df}{A data frame combining multiple get_dist() outputs, with ROI
and group columns added.}

\item{roi_col}{Character. Name of the ROI identifier column
(default \code{"roi"}).}

\item{group_col}{Character. Name of the group identifier column
(default \code{"group"}).}

\item{origin_id_col}{Character. Column for the origin cell ID
(default \code{"origin_name"}).}

\item{origin_type_col}{Character. Column for the origin cell type / value
(default \code{"origin_value"}).}

\item{neighbor_id_col}{Character. Column for the neighbor cell ID
(default \code{"neighbor_name"}).}

\item{neighbor_type_col}{Character. Column for the neighbor cell type / value
(default \code{"neighbor_value"}).}

\item{distance_col}{Character. Column for the pairwise distance
(default \code{"distance"}).}
}
\value{
A standardized data frame with columns:
  \code{roi}, \code{group}, \code{origin_id}, \code{origin_type},
  \code{neighbor_id}, \code{neighbor_type}, \code{distance}.
  Rows with any \code{NA} in these columns are removed.
}
\description{
Renames and coerces columns to a canonical format required by the
downstream neighborhood analysis pipeline.
}
